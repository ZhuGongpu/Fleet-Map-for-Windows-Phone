<phone:PhoneApplicationPage
    x:Class="FleetMap.NewMarkerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">


    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar>
            <shell:ApplicationBarIconButton x:Name="PostMarker" IconUri="Assets/Check.png" IsEnabled="True"
                                            Text="完成" Click="PostMarker_OnClick" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,17">
            <TextBlock Text="发布" Style="{StaticResource PhoneTextNormalStyle}" />
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" >

            <TextBox
                Name="TextBox"
                HorizontalAlignment="Left"
                Height="165"
                TextWrapping="Wrap"
                VerticalAlignment="Top"
                Width="480" />

            <Image Name="Image"
                   HorizontalAlignment="Left"
                   Height="165"
                   Margin="10,179,0,0"
                   VerticalAlignment="Top"
                   Width="164" OpacityMask="#FFF9F5F5" Stretch="UniformToFill" 
                   />
            <Button Name="PickImage" Content="上传图片" 
                    HorizontalAlignment="Left" Margin="233,229,0,0" VerticalAlignment="Top"
                    Click="PickImage_OnClick"/>

        </Grid>


    </Grid>

</phone:PhoneApplicationPage>